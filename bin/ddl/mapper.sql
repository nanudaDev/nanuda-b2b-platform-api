-- 업체 및  가격표 매퍼
CREATE TABLE `nanuda_platform_test`.`B2B_COMPANY_PRICING_MAPPER` (
  `NO` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `COMPANY_NO` INT NOT NULL,
  `COMPANY_PRICING_NO` INT NOT NULL,
  `createdAt` DATETIME NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`NO`),
  UNIQUE INDEX `NO_UNIQUE` (`NO` ASC) VISIBLE)
COMMENT = 'pricing table 매퍼';


-- 지점 및 시설 매퍼
CREATE TABLE `B2B_COMPANY_DISTRICT_AMENITY_MAPPER` (
  `NO` int unsigned NOT NULL AUTO_INCREMENT,
  `COMPANY_NO` int NOT NULL,
  `COMPANY_DISTRICT_NO` int NOT NULL,
  `COMPANY_USER_NO` int DEFAULT NULL,
  `ADMIN_NO` int DEFAULT NULL,
  `AMENITY_NO` int NOT NULL,
  `createdAt` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`NO`),
  UNIQUE KEY `NO_UNIQUE` (`NO`)
) ENGINE=InnoDB AUTO_INCREMENT=6 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='지점별 공용 시설 매퍼'